#pragma once

#include <iostream>
#include <string>
#include <vector>
#include <ctime>
#include <string>

using Matrix = std::vector<std::vector<int>>;
using CharMatrix = std::vector<std::vector<char>>;